// config/botFaq.ts
export const QUICK_REPLIES: string[] = [
  "ติดต่อพนักงาน",
  "ช่วยเหลือ",
  "สั่งซื้อ",
  "โปรโมชั่น",
  "เปิดบัญชีเทรดเพื่อรับสิทธิ",
];

const ANSWERS = {
  contact:
    "หากต้องการติดต่อทีมงานโดยตรง สามารถทิ้งเบอร์ไว้ได้เลยครับ เราจะติดต่อกลับโดยเร็วที่สุด",
  help:
    "ทีม Longtrade Support พร้อมช่วยคุณทุกวัน หากมีคำถามสามารถพิมพ์สอบถามได้เลยครับ",
  order:
    "หากต้องการสั่งซื้อ สามารถกด “สั่งซื้อ” ได้เลย หลังชำระค่าสินค้าเสร็จส่งหลักฐานการชำระมาที่ ID Line : @Longtrade  หรือแจ้งทีมงานเพื่อช่วยดูแลขั้นตอนครับ",
  promo:
    'ตอนนี้มีโปรโมชั่นพิเศษทั้งสำหรับสมาชิกใหม่และโบนัสเทรด สนใจกดดูที่เมนู "โปรโมชั่น" ในหน้าเว็บได้เลยครับ',
  open:
    "กดที่ปุ่มเปิดบัญชีเทรดกับ XM (หน้าเว็ป) ได้เลย หรือ\n📌 เปิดบัญชีผ่านลิงก์นี้ ➡️ https://affs.click/iQ1BQ\n.\n⚠️  อย่าลืมกรอกรหัสพันธมิตร : LONGTRADE\n⚠️  โดย คลิกคำว่า \"ป้อนที่นี้\"\n⚠️  เปิดเสร็จ พี่แจ้งเลข บัญชีเทรด ผมนะเดี๋ยวผมดำเนินการ ต่อให้ครับ",
};

function match(input: string, keys: string[]) {
  const q = input.toLowerCase();
  return keys.some((k) => q.includes(k));
}

export function getBotReply(input: string): string {
  const q = input.trim();

  if (match(q, ["ติดต่อ", "พนักงาน", "แอดมิน", "คอลเซ็นเตอร์", "contact"]))
    return ANSWERS.contact;

  if (match(q, ["ช่วย", "ช่วยเหลือ", "help", "support", "ซัพพอร์ต"]))
    return ANSWERS.help;

  if (match(q, ["สั่งซื้อ", "ซื้อ", "order", "จ่าย", "ชำระ"]))
    return ANSWERS.order;

  if (match(q, ["โปร", "โปรโมชั่น", "promotion", "bonus", "โบนัส"]))
    return ANSWERS.promo;

  if (match(q, ["เปิดบัญชี", "เปิดพอร์ต", "บัญชีเทรด", "พอร์ต", "register"]))
    return ANSWERS.open;

  // ค่าพื้นฐานเมื่อจับไม่ตรงหัวข้อ
  return (
    'เลือกหัวข้อด้านบนหรือพิมพ์คำว่า "ติดต่อพนักงาน", "ช่วยเหลือ", "สั่งซื้อ", "โปรโมชั่น", หรือ "เปิดบัญชีเทรดเพื่อรับสิทธิ" ได้เลยครับ'
  );
}
